/**
 * This is an angular compatible object that should return a Q promise
 *
 */
import CacheConfig from './config/cache';

class RestAngularCache {

    constructor(private config: CacheConfig){

    }

    
}
